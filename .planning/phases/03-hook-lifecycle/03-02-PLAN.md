---
phase: 03-hook-lifecycle
plan: 02
type: execute
---

<objective>
Implement `dgent uninstall` and `--dry-run` mode.

Purpose: Complete the hook lifecycle — users can install and cleanly remove dgent, and preview changes without applying.
Output: Working `dgent uninstall`, `--dry-run` flag, `DGENT_DRY_RUN` env var support.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@ARCHITECTURE.md
@.planning/phases/03-hook-lifecycle/03-01-SUMMARY.md
@src/commands/uninstall.ts
@src/hooks/install.ts
@src/hooks/commit-msg.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement dgent uninstall</name>
  <files>src/commands/uninstall.ts, src/hooks/install.ts</files>
  <action>
    Add to src/hooks/install.ts:
    - `uninstallHooks()`:
      1. Check if core.hooksPath is set and owned by dgent
      2. If owned by dgent: unset core.hooksPath (`git config --global --unset core.hooksPath`), remove hooks directory
      3. If not owned by dgent: warn that hooks aren't managed by dgent, exit without changes
      4. If not set: print "dgent hooks not installed"
    - Print confirmation on success

    Wire src/commands/uninstall.ts to call uninstallHooks().
  </action>
  <verify>`dgent init` then `dgent uninstall` removes hooks and unsets core.hooksPath. `git config --global core.hooksPath` returns empty after uninstall.</verify>
  <done>dgent uninstall cleanly removes hooks, unsets core.hooksPath, handles edge cases</done>
</task>

<task type="auto">
  <name>Task 2: Implement --dry-run mode</name>
  <files>src/hooks/commit-msg.ts, src/commands/hook.ts</files>
  <action>
    Add dry-run support to the hook handler:
    - Check for DGENT_DRY_RUN=1 environment variable
    - In dry-run mode: run all rules but don't write the modified file back
    - Instead, print what would change:
      - Which rules fired
      - Diff of original vs modified message (if any fix rules changed it)
      - Flags that would be produced
    - Print "[dry-run] No changes applied" at the end

    This allows: `DGENT_DRY_RUN=1 git commit -m "..."` to preview without applying.
  </action>
  <verify>`DGENT_DRY_RUN=1 dgent hook commit-msg /tmp/test-msg.txt` shows changes without modifying the file. File contents unchanged after dry-run.</verify>
  <done>Dry-run mode previews all changes without applying, triggered by DGENT_DRY_RUN=1 env var</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `dgent uninstall` removes hooks cleanly
- [ ] `dgent uninstall` when not installed prints appropriate message
- [ ] Dry-run mode shows changes without modifying files
- [ ] Phase 3 complete — full hook lifecycle working
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- Install, uninstall, and dry-run all work correctly
- Phase 3 complete
</success_criteria>

<output>
After completion, create `.planning/phases/03-hook-lifecycle/03-02-SUMMARY.md`
</output>
