---
phase: 06-tui-and-polish
plan: 01
type: execute
---

<objective>
Implement dgent run (manual pass), dgent log (flag history), and professional CLI branding with ASCII art, colors, and polished output.

Purpose: Make dgent feel like a real tool — professional output, branding, and the two missing utility commands.
Output: Working dgent run, dgent log, branded CLI output with art and colors.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@ARCHITECTURE.md
@src/commands/run.ts
@src/commands/log.ts
@src/rules/index.ts
@src/hooks/commit-msg.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: CLI branding — ASCII art, colors, and polished output</name>
  <files>src/ui/brand.ts, src/ui/colors.ts, src/cli.ts, src/commands/init.ts, src/testing/runner.ts</files>
  <action>
    Create src/ui/colors.ts with ANSI color helpers (no dependency):
    - bold, dim, red, green, yellow, cyan, gray
    - Auto-detect NO_COLOR env var and strip colors if set
    - Keep it minimal — just template tag functions for ANSI codes

    Create src/ui/brand.ts with:
    - ASCII art logo for dgent (clean, compact, monospace)
    - Version string
    - `printBanner()` — prints logo + version, used on init and help
    - `printSuccess(msg)`, `printWarning(msg)`, `printError(msg)` — consistent styled output
    - `printFlag(flag)` — formatted flag output with file:line prefix
    - `printDiff(before, after)` — minimal diff view with red/green coloring
    - `printRuleResult(name, changed, flagCount)` — per-rule summary line

    Update src/cli.ts:
    - Show banner on `dgent --help`
    - Show version with banner art on `dgent --version`

    Update dgent init output to use branded messages.

    Update dgent test output to use colors:
    - Green checkmarks, red X marks
    - Summary line with pass/fail counts in color
    - Rule names in cyan

    Update hook output (commit-msg, pre-commit) to use branded flag/warning formatting.
  </action>
  <verify>`dgent --help` shows branded output with art, `dgent test` shows colored results, `dgent init` shows polished messages</verify>
  <done>Professional CLI appearance with ASCII art, colored output, consistent formatting across all commands</done>
</task>

<task type="auto">
  <name>Task 2: Implement dgent run and dgent log</name>
  <files>src/commands/run.ts, src/commands/log.ts, src/hooks/log-writer.ts</files>
  <action>
    **dgent run:**
    - `dgent run <file>` — reads file, runs all rules (both commit-msg and pre-commit), prints results
    - `dgent run -` — reads from stdin
    - `dgent run --dry-run <file>` — preview only
    - Output: for each rule that fires, show the rule name and what changed/flagged
    - If fix rules modified content, print the cleaned output to stdout (pipeable)
    - If flag rules found issues, print flags to stderr

    **dgent log:**
    Create src/hooks/log-writer.ts:
    - `writeLog(flags, commitHash?)`: writes flags to `~/.local/share/dgent/logs/` as JSON files
    - Filename: `YYYY-MM-DD-HHmmss.json`
    - Content: `{ timestamp, commit, flags[] }`

    Wire log-writer into commit-msg and pre-commit handlers — after processing, if flags were produced, write a log entry.

    `dgent log`:
    - Reads recent log files from `~/.local/share/dgent/logs/`
    - Prints last 10 entries with timestamp, commit hash (if available), and flag summary
    - Uses branded formatting
    - `dgent log --all` shows everything

    Both commands use branded output with colors.
  </action>
  <verify>`dgent run src/rules/index.ts` processes the file and shows results. `dgent log` shows recent flags (empty if none yet). After running dgent with real hooks, log entries appear.</verify>
  <done>dgent run processes files manually, dgent log shows flag history, logs persist to ~/.local/share/dgent/logs/</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `dgent --help` shows branded ASCII art
- [ ] `dgent test` output is colored and professional
- [ ] `dgent run <file>` processes a file and shows results
- [ ] `dgent run -` reads from stdin
- [ ] `dgent log` shows recent flag history
- [ ] Flag logs persist to disk
- [ ] All 44 fixtures still pass
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- dgent looks and feels like a polished CLI tool
</success_criteria>

<output>
After completion, create `.planning/phases/06-tui-and-polish/06-01-SUMMARY.md`
</output>
