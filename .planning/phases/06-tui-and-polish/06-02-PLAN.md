---
phase: 06-tui-and-polish
plan: 02
type: execute
---

<objective>
Implement per-repo overrides (.dgent.json), self-update (dgent update), and final polish.

Purpose: Complete the feature set — per-repo config customization and self-update mechanism.
Output: .dgent.json support, dgent update via npm, npm publish readiness. Project complete.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@ARCHITECTURE.md
@.planning/phases/06-tui-and-polish/06-01-SUMMARY.md
@src/config/index.ts
@src/commands/update.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Per-repo rule overrides via .dgent.json</name>
  <files>src/config/index.ts, src/hooks/commit-msg.ts, src/hooks/pre-commit.ts</files>
  <action>
    Update config loading to check for `.dgent.json` in the current git repo root:
    1. `getRepoRoot()`: run `git rev-parse --show-toplevel` to find repo root
    2. Check for `.dgent.json` at repo root
    3. If exists, deep-merge its `rules` section with global config (repo overrides global)
    4. Only the `rules` key is overridable per-repo (not ai or output settings)

    Example `.dgent.json`:
    ```json
    {
      "rules": {
        "strip-noise-comments": true,
        "flag-naming": false
      }
    }
    ```

    This enables a project to opt into strip-noise-comments even though it's globally off, or disable flag-naming for a repo that legitimately uses Service/Manager naming.

    Update both hook handlers to use the merged config.
  </action>
  <verify>Create a test .dgent.json, verify rules are overridden when running dgent in that repo</verify>
  <done>Per-repo overrides work, deep-merge with global config, only rules section overridable</done>
</task>

<task type="auto">
  <name>Task 2: Self-update and npm publish readiness</name>
  <files>src/commands/update.ts, package.json</files>
  <action>
    **dgent update:**
    - Runs `npm update -g dgent` to self-update
    - Before updating, checks current version vs latest on npm registry
    - If already latest, prints "Already up to date (vX.Y.Z)"
    - If update available, prints "Updating dgent vX.Y.Z → vA.B.C..." and runs the update
    - Uses `npm view dgent version` to check latest

    **npm publish readiness:**
    - Ensure package.json has: files field listing what to include (src/, dist/, test/fixtures/, README)
    - Add "prepublishOnly": "npm run build" to scripts
    - Add "keywords" for npm discoverability
    - Verify `npm pack --dry-run` shows correct files

    NOTE: Do NOT actually publish. Just make it ready.
  </action>
  <verify>`dgent update` checks for updates (will show "not published" for now). `npm pack --dry-run` lists correct files.</verify>
  <done>Self-update command works, package.json ready for npm publish. Phase 6 and project complete.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] .dgent.json overrides work
- [ ] dgent update checks npm registry
- [ ] npm pack --dry-run lists correct files
- [ ] All 44 fixtures still pass
- [ ] All commands work end-to-end
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- Phase 6 complete — project feature-complete
</success_criteria>

<output>
After completion, create `.planning/phases/06-tui-and-polish/06-02-SUMMARY.md`
</output>
