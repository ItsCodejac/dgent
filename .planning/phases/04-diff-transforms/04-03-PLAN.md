---
phase: 04-diff-transforms
plan: 03
type: execute
---

<objective>
Implement flag-naming and flag-catch-rethrow pre-commit rules with fixtures.

Purpose: Complete the default-on diff rule set. Both are flag rules — they warn without modifying code.
Output: Two working flag rules with fixtures. Phase 4 complete.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@ARCHITECTURE.md
@.planning/phases/04-diff-transforms/04-02-SUMMARY.md
@src/rules/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement flag-naming rule with fixtures</name>
  <files>src/rules/flag-naming.ts, test/fixtures/flag-naming/*.ts</files>
  <action>
    Create src/rules/flag-naming.ts:
    - name: "flag-naming", phase: "pre-commit", type: "flag", defaultEnabled: true
    - Scans for AI naming patterns on declaration lines:
      - Suffix clusters: Manager, Handler, Processor, Service, Factory, Builder, Validator, Controller, Orchestrator, Coordinator
      - Match on lines containing: class, function, const, let, var, def, interface, type declarations
      - Only flag the identifier, not every mention
    - Also flags overlong identifiers:
      - Configurable threshold (default: 40 characters)
      - Only on declaration lines (same detection as above)
    - Uses regex on declaration lines — no AST required
    - Returns flags with the identifier name and suggestion

    Register in rules array.

    Create fixtures:
    - ai-naming-patterns.input.ts / .expected.ts — class DataProcessor, function UserServiceHandler (expected = input, flags expected)
    - overlong-identifier.input.ts / .expected.ts — const processAndValidateUserInputData (expected = input, flag for length)
    - normal-names.input.ts / .expected.ts — class User, function getById (expected = input, no flags)
    - false-positive-valid-service.input.ts / .expected.ts — common framework patterns like Express Router (should still flag — dgent is opinionated)
  </action>
  <verify>`dgent test --rule flag-naming` passes all fixtures</verify>
  <done>flag-naming detects AI naming patterns and overlong identifiers on declaration lines, all fixtures pass</done>
</task>

<task type="auto">
  <name>Task 2: Implement flag-catch-rethrow rule with fixtures</name>
  <files>src/rules/flag-catch-rethrow.ts, test/fixtures/flag-catch-rethrow/*.ts</files>
  <action>
    Create src/rules/flag-catch-rethrow.ts:
    - name: "flag-catch-rethrow", phase: "pre-commit", type: "flag", defaultEnabled: true
    - Detects catch blocks that only log and re-throw:
      - Pattern: catch block containing console.error/console.log/console.warn + throw, with nothing else meaningful
      - Heuristic: catch block body has <= 3 lines, contains a log statement and a throw/raise
    - Works across JS/TS (`catch (e)`) and Python (`except ... as e:`)
    - Flags with message explaining the pattern adds no value

    Register in rules array.

    Create fixtures:
    - basic-catch-rethrow.input.ts / .expected.ts — catch (e) { console.error(e); throw e; } (flag)
    - catch-with-context.input.ts / .expected.ts — catch that adds context before rethrowing (no flag)
    - catch-handles-error.input.ts / .expected.ts — catch that actually handles the error (no flag)
    - no-catch-blocks.input.ts / .expected.ts — clean code (no flags)
  </action>
  <verify>`dgent test --rule flag-catch-rethrow` passes all fixtures</verify>
  <done>flag-catch-rethrow detects empty catch-and-rethrow patterns, distinguishes from meaningful error handling, all fixtures pass. Phase 4 complete.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run build` succeeds
- [ ] `dgent test` runs all fixtures across all rules
- [ ] All fixtures pass
- [ ] Phase 4 complete — all default-on diff rules working
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- All 8 rules (4 commit-msg + 4 pre-commit) working with fixtures
- Phase 4 complete
</success_criteria>

<output>
After completion, create `.planning/phases/04-diff-transforms/04-03-SUMMARY.md`
</output>
